{
  "name": "order-receiver",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "build": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Remove-Item -LiteralPath 'order_receiver.zip' -Force -ErrorAction SilentlyContinue; Compress-Archive -Path index.mjs,package.json,node_modules -DestinationPath order_receiver.zip -Force\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.918.0",
    "@aws-sdk/client-sqs": "^3.918.0",
    "@aws-sdk/lib-dynamodb": "^3.918.0"
  }
}
